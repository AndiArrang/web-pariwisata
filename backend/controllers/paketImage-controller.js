

const upload = async (req,res,next) => {
    // try {
    //     const result = await uploadPaketImage(req.file,req.params.idPaket)

    //     if(result) {
    //         res.status(200).json({
    //             status: 'success',
    //             message: 'Data added successfully.',
    //             data: result
    //         })
    //     }
    // } catch (error) {
    //     next(error)
    // }
}

export default {
    upload
}